
torch>=2.0.0
tensorboard>=2.10.0
numpy>=1.24.0
scipy>=1.10.0
scikit-learn>=1.2.0

sympy>=1.12
mpmath>=1.3.0

hydra-core>=1.3.0
omegaconf>=2.3.0
faiss-cpu>=1.7.0
tqdm>=4.65.0

sqlalchemy>=2.0.0

matplotlib>=3.5.0
seaborn>=0.12.0

pyyaml>=6.0

# OCR pipeline (corpus/ocr) — only needed on the GPU box that hosts the vLLM
# server. The Python OCR-client code lives in the base requirements
# (pymupdf, openai); vLLM itself is the heavy part.
vllm>=0.7.0
