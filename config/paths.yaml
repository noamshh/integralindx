project_root: "."

config:
  root: "config"
  api_keys: "config/api_keys.yaml"

data:
  root: "data"
  raw:
    root: "data/raw"
    formulas: "data/raw/formulas"
    blobs: "data/raw/blobs"
    answer_blobs: "data/raw/answer_blobs"
  contributors_rebuild:
    root: "data/contributors_rebuild"
    filtered: "data/contributors_rebuild/filtered"
    var_normalized: "data/contributors_rebuild/var_normalized"
    canonicalized: "data/contributors_rebuild/canonicalized"
    grouped: "data/contributors_rebuild/grouped"
  legacy: "data/legacy"
  ml_training: "data/ml_training"
  ml_seeds: "data/ml_seeds"
  normalized: "data/normalized"
  integral_db: "data/integral.db"

ml_training:
  root: "data/ml_training"
  seeds: "data/ml_seeds/synthetic_seeds.txt"

models:
  checkpoints: "data/models/checkpoints"
  runs: "data/models/runs"  # tensorboard logs

egraph:
  binary: "bin/egen"

files:
  contributing_source_ids: "data/contributing_source_ids.txt"
  mse_raw: "data/raw/formulas/mse.jsonl"
  contributors_normalized: "data/contributors_rebuild/normalized_contributors.jsonl"
  contributors_grouped_dir: "data/contributors_rebuild/grouped"
  integrand_groups: "data/contributors_rebuild/grouped/integrand_groups.jsonl"
  integrals_all_grouped: "data/contributors_rebuild/grouped/integrals_all.jsonl"
  normalized_accepted: "data/normalized/normalized_accepted.jsonl"
  normalized_rejected: "data/normalized/normalized_rejected.jsonl"
