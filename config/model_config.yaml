# encoder-only transformer with mean pooling
# based on E-Gen implementation

model:
  name: "integralindx-contrastive"
  encoder:
    dim: 512
    num_layers: 6
    num_heads: 8
    feedforward_dim: 2048
    dropout: 0.1
    max_seq_len: 200
    vocab_size: null
  pooling: "mean"