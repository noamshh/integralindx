# ii-cl-19m: IntegralIndx Contrastive Learning 19M parameters
name: "ii-cl-19m"
version: "v1.0"
encoder:
  dim: 512
  num_layers: 6
  num_heads: 8
  feedforward_dim: 2048
  dropout: 0.1
  max_seq_len: 150
  vocab_size: null  # set at runtime from tokenizer
pooling: "mean"
